# 2026-07 — Inbox

## 2026-07-03 12:00 — self-evolution init scan of create-loop

- The repo is a **skill + delivery machinery**, not a runtime. Two artifact classes coexist: Node installer/rendered-commands, and the packaged Python+Markdown skill under `skills/create-loop/`. Most "code" is actually docs/schemas: 74 json, 52 md, 24 yaml, 18 py, only 2 js files [source: .agents/knowledge/reference/.project-scan.txt CODE METRICS].
- No CI is committed (`.github/workflows/` absent) even though two natural gates exist: `npm test` (installer) and `skills/create-loop/tests/acceptance_tests.md` (skill). Worth confirming whether CI is intended [source: .agents/knowledge/reference/.project-scan.txt CI/CD].
- Nested AGENTS.md files carry real, high-signal invariants at three levels: repo root, `command/`, `skills/create-loop/`, and `skills/create-loop/scripts/`. These were imported into domain files as `confidence: observed` [source: AGENTS.md, command/AGENTS.md, skills/create-loop/AGENTS.md, skills/create-loop/scripts/AGENTS.md].

## 2026-07-03 12:05 — Intent vs Reality check (docs vs code)

- No divergences found between README/AGENTS claims and code for the two headline models (source-vs-rendered commands; zero-dep installer). Verified `render` reproduces committed host files byte-for-byte via the live test run (15/15 pass) [source: test/installer.test.js:81-91].
- Subtle point that could confuse a new contributor: `install-commands.sh` (Path B) does NOT render — it copies the already-rendered `.opencode/command/` and `.claude/commands/` files. Only `bin/create-loop.js render` regenerates them. Editing `command/` then running `install-commands.sh` without re-rendering ships stale commands [source: install-commands.sh:92-99, bin/create-loop.js:429-449].
- The Python validator is authoritative and the 11 JSON schemas mirror it; `scripts/checks/__init__.py` is the single source of truth for every enum/regex/required tuple. Any schema/validator drift is a defect [source: skills/create-loop/scripts/AGENTS.md:35,43].

## 2026-07-03 12:10 — self-evolution skill feedback

- [SKILL-IDEA:self-evolution] `init-scaffold.sh` emits a benign but ugly error on its final hint block: `printf: Illegal option - ` (the POSIX `printf` in `dash`/`sh` mis-parses a hint line beginning with `-`). All files were still created correctly, but the trailing hints are truncated. Suggested repair: prefix those `printf` format strings with `--` or use `printf '%s\n' "..."` for lines that can begin with a hyphen [source: init-scaffold.sh:558-563, observed run 2026-07-03].
- Impact: cosmetic only (scaffold succeeds), but the truncated POST-SCAFFOLD HINTS reduce the script's usefulness on `sh`/`dash` systems.

## 2026-07-03 12:20 — Hook integration wiring (opencode) + skill feedback

- Wired the opencode hook adapter per user choice. `install-hooks.sh` copies the GLOBAL `~/.config/opencode/opencode.json` into `.opencode/opencode.json` to preserve providers/plugins — but the global config's `plugin` array carried a stale absolute path from a DIFFERENT project (`file:///root/alpha/.agents/hooks/opencode-plugin.mjs`). The installer's idempotency check matched on the plugin filename and reported "already references the plugin; skipping", so it never corrected the path. Fixed manually to `file:///root/create-loop/...` [source: .opencode/opencode.json:3, install-hooks.sh output].
- [SKILL-FIX:self-evolution] `install-hooks.sh` opencode path: when the copied global config already contains an `opencode-plugin.mjs` entry pointing at ANOTHER project's absolute path, the installer skips instead of rewriting it to the current project root. Result: hooks silently point at the wrong project and never fire here. Suggested repair: match the plugin entry by basename and REWRITE its `file://` URL to `file://<current project root>/.agents/hooks/opencode-plugin.mjs` rather than skipping when any `opencode-plugin.mjs` entry exists.
- Impact: hooks recorded as "active" would have been non-functional (wrong absolute path) without manual correction — exactly the "installed but not wired" failure the decision point is meant to prevent.
- Note: `.opencode/opencode.json` is gitignored (.gitignore:24), so this adapter is local-only and won't be committed — acceptable since it carries machine-specific provider credentials.
